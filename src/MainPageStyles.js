import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  textheading: {
    textAlign: "center",
    fontSize: "36px",
    fontWeight: "bold",
    fontStyle: "normal",
  },
  headingthree: {
      width: '439px'
  }
}));
